import org.gradle.buster.BusterPlugin

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath files('../build/libs/gradle-buster-plugin-0.1-SNAPSHOT.jar')
    }
}


apply plugin: 'buster'

// configure buster options
buster {
    port = 1112 // override default
}